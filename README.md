# Portfolio Projetos

Este é um projeto de portfólio desenvolvido com as tecnologias mais recentes do ecossistema React. O objetivo é demonstrar habilidades no desenvolvimento de interfaces modernas, performáticas e responsivas.

## 🚀 Tecnologias Utilizadas

O projeto foi construído utilizando a seguinte stack tecnológica:

-   **[Next.js 16](https://nextjs.org/)**: Framework React para produção, utilizando o **App Router** para roteamento e otimizações de servidor.
-   **[React 19](https://react.dev/)**: Biblioteca JavaScript para construção de interfaces de usuário.
-   **[TypeScript](https://www.typescriptlang.org/)**: Superset do JavaScript que adiciona tipagem estática, garantindo maior segurança e escalabilidade ao código.
-   **[Tailwind CSS v4](https://tailwindcss.com/)**: Framework de CSS utility-first para estilização rápida e responsiva.
-   **[React Icons](https://react-icons.github.io/react-icons/)**: Biblioteca de ícones populares para React.
-   **[ESLint](https://eslint.org/)**: Ferramenta de linting para identificar e corrigir problemas no código JavaScript/TypeScript.

## 📂 Estrutura do Projeto

A estrutura do projeto segue o padrão moderno do Next.js:

```
portfolio_projetos/
├── src/
│   ├── app/          # Rotas e layouts da aplicação (App Router)
│   ├── components/   # Componentes reutilizáveis
├── public/           # Arquivos estáticos
├── package.json      # Dependências e scripts
└── ...
```

## 🛠️ Como Executar

Para rodar o projeto localmente, siga os passos abaixo:

1.  **Instale as dependências:**

    ```bash
    npm install
    # ou
    yarn install
    # ou
    pnpm install
    ```

2.  **Inicie o servidor de desenvolvimento:**

    ```bash
    npm run dev
    # ou
    yarn dev
    # ou
    pnpm dev
    ```

3.  Acesse [http://localhost:3000](http://localhost:3000) no seu navegador para ver o resultado.

## 📜 Scripts Disponíveis

-   `dev`: Inicia o servidor de desenvolvimento.
-   `build`: Cria a build de produção.
-   `start`: Inicia o servidor de produção.
-   `lint`: Executa o ESLint para verificação de código.
